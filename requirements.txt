pandas  # pinned <2.2.0 for Hopsworks compatibility (see note below)
numpy
requests
python-dotenv
scikit-learn
tensorflow
protobuf # pinned: must satisfy both TensorFlow 2.16 (<5.0) and
                  # Hopsworks (>=4.25.4,<5.0). Mismatched protobuf versions
                  # can crash the interpreter on import — keep this pin.
joblib
pyarrow
shap
streamlit
flask
flask-cors
plotly
matplotlib
pydantic
pytest
pytest-mock
tqdm
PyYAML

# Hopsworks Feature Store client (default feature store backend for this
# project — see src/features/feature_store.py). The [python] extra pulls in
# the offline "python engine" (fastavro, confluent-kafka) needed to read/
# write feature groups without Spark.
#
# NOTE: hopsworks pins pandas<2.2.0, which is why pandas is pinned to 2.1.4
# above instead of a newer release — this is intentional, not an oversight.
hopsworks[python]
