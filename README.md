# AQI Predictor

Serverless AQI forecasting pipeline. In progress.

## Setup
1. Copy `.env.example` to `.env` and fill in your API key
2. `pip install -r requirements.txt`
3. `python feature_pipeline.py`